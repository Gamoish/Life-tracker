node_modules
out
.next
next-env.d.ts
*.tsbuildinfo

# Android build outputs — the project files themselves (android/) are checked
# in, since `cap add android` doesn't need to be re-run on every machine.
android/app/build
android/build
android/.gradle
android/local.properties
android/captures
android/.cxx
*.apk
*.aab
